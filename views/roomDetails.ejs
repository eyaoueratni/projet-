
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= room.name %></title>
    <link rel="stylesheet" href="/css/index.css" />
    <style>
      /* Réduire la taille des carrés de salle */
      .room-square {
        width: 200px;
        height: 200px;
        margin: 20px;
        background-color: #f0f0f0;
        display: flex;
        align-items: center;
        justify-content: center;
        border: 2px solid #ccc;
        cursor: pointer;
        transition: background-color 0.3s, transform 0.3s;
        border-radius: 10px;
        font-family: Arial, sans-serif;
        font-size: 18px;
        color: #333;
      }
      /* Style des carrés de salle au survol */
      .room-square:hover {
        background-color: #e0e0e0;
        transform: scale(1.05);
      }
      /* Container pour les carrés de salle */
      .room-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 50px;
      }
      /* Style du titre */
      h1 {
        text-align: center;
        color: #333;
        font-size: 36px;
        font-weight: bold;
        margin-bottom: 30px;
        text-transform: uppercase;
        letter-spacing: 2px;
      }
      /* Style du cadre */
      .info-section {
        border: 2px solid black;
        border-radius: 10px;
        padding: 30px;
        margin-bottom: 30px;
        background-color: #f9f9f9;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }
      .info-section h2 {
        color: black;
        font-size: 24px;
        font-weight: bold;
        margin-bottom: 20px;
        text-align: center;
        text-transform: uppercase;
      }
      /* Style des détails de la salle */
      .room-details {
        margin-bottom: 20px;
      }
      .room-details label {
        font-weight: bold;
        color: #555;
        margin-bottom: 10px;
        display: block;
        font-size: 16px;
      }
      .room-details p {
        color: #333;
        margin-bottom: 15px;
        font-size: 18px;
      }
      /* Style du bouton */
      .button-container {
        display: flex;
        justify-content: center;
        margin-top: 30px;
      }
      .button {
        background-color: black;
        color: #fff;
        padding: 14px 28px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        text-decoration: none;
        transition: background-color 0.3s;
        margin: 0 15px;
        font-size: 18px;
        font-weight: bold;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        outline: none;
      }
      .button:hover {
        background-color: black;
      }
      /* Style du lien de retour */
      .back-link {
        display: block;
        text-align: center;
        margin-top: 20px;
        color: black;
        text-decoration: none;
        font-size: 16px;
        font-weight: bold;
        transition: color 0.3s;
      }
      .back-link:hover {
        color: #0056b3;
      }
      .room-details {
  margin-bottom: 20px;
}

.detail-item {
  margin-bottom: 15px;
}

.detail-item label {
  font-weight: bold;
  color:orangered;
  margin-bottom: 5px;
  display: block;
}

.detail-item p {
  color: #333;
  margin: 0;
  font-size: 16px;
}

    </style>
  </head>
  <body>
    
    <h1>Room: <%= room.name %></h1>

<div class="info-section">
  <h2>Room Information</h2>
  <div class="room-details">
    <div class="detail-item">
      <label>Room Name:</label>
      <p><%= room.name %></p>
    </div>
    <div class="detail-item">
      <label>Floor:</label>
      <p><%= room.floor %></p>
    </div>
    <div class="detail-item">
      <label>Capacity:</label>
      <p><%= room.capacity %> persons</p>
    </div>
    <div class="detail-item">
      <label>Assets:</label>
      <p><%= room.assets %></p>
    </div>
    <div class="detail-item">
      <label>Status:</label>
      <p><%= room.availability? "Available" : "Unavailable" %></p>
    </div>
  </div>
</div>

<div class="button-container">
    <!-- Button to view calendar -->
    <a href="/calendar/<%= room._id %>" class="button">View Calendar</a>

 <!-- Button to reserve the room -->
 <a href="/reserve/<%= room._id %>" class="button">Reserve Room</a>
</div>     
    <a href="/rooms" class="back-link">Back to all rooms</a>

  
</html>
  </body>
</html>